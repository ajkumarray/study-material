package dev.ajay.expenseapi.domain;

/** Spending buckets (stored by name via @Enumerated(STRING), Phase 4). */
public enum Category {
    FOOD, TRANSPORT, RENT, ENTERTAINMENT, UTILITIES, HEALTH, OTHER
}
